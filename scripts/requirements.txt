# Zaleznosci Pythona dla uslug serwera Smart Home (instalowane w scripts/.venv przez setup).
# Wersje przypiete dla powtarzalnosci na Ubuntu Server / arm64 (Pi Zero 2W).
paho-mqtt==1.6.1
Flask==3.0.3
# Opcjonalne – tylko dla wyswietlacza LCD I2C (import owiniety try/except w lcd_display.py):
RPLCD==1.3.0
smbus2==0.4.3
